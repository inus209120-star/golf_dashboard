# 골프장 현황 대시보드 — PRD (Product Requirements Document)

문서 버전: v0.1 (초안) · 작성일: 2026-09-08
대상 골프장: (주)오션디앤씨 스톤게이트CC (부산 기장군)

---

## 1. 배경 및 문제 정의

대표님은 서울에서 근무하며 부산 기장군 골프장 현장과 물리적으로 떨어져 있다. 현재는 현장의 수기 보고에 의존해 매출, 예약률, 자금 흐름을 파악하고 있어, 확인 시점이 보고 주기(주간/월간)에 종속되고 보고 누락·왜곡·지연 리스크가 존재한다.

현장에는 두 개의 시스템이 있다.
- **무노스** (예약/매출 관리 프로그램): 예약, 티타임, 매출 실적 데이터
- **더존** (회계 프로그램): 자금수지, 계정과목별 재무 데이터

두 시스템 모두 엑셀(xls/xlsx) 다운로드는 가능하나 API 연동은 즉시 불가하다. 따라서 **1단계는 "현장 직원이 엑셀을 다운로드해 웹에 업로드하면 자동으로 집계·시각화되는" 구조**로 설계하고, 개발 리소스가 합류하면 API 직접 연동으로 고도화한다.

### 1.1 목표
- 대표님이 로그인 절차 없이, 서울에서든 어디서든 브라우저 링크 하나로 최신 매출·예약·자금·그린피 단가 현황을 확인
- 현장 직원의 업무 부담을 "다운로드 + 업로드" 수준으로 최소화 (수기 재입력 없음, 그린피 단가만 월 1회 소량 수기 입력)
- 새로고침해도 데이터가 사라지지 않는 영속 저장 구조
- 서버·DB를 별도로 구축하지 않는 기술적으로 단순한 1단계 구조로 지금 바로 시작

### 1.2 성공 지표 (KPI)

| 지표 | 목표 |
|---|---|
| 대표님의 수기 보고 의존도 | 주 1회 이하로 감소 |
| 전일 데이터 반영 시점 | 익일 오전 업무 시작 전 |
| 현장 직원의 업로드 소요 시간 | 1일 5분 이내 |
| 데이터 정확도 | 원본 대비 오차 0% (파싱 오류 자동 감지) |

### 1.3 핵심 기능 (대표님 요청 6종)

| # | 기능 | 데이터 출처 | 갱신 방식 |
|---|---|---|---|
| 1 | 자금 현황 (일일 자금수지) | 더존 – 합계잔액시산표(자금일보) | 파일 업로드 |
| 2 | 일일 매출 | 무노스 – 일일영업집계 | 파일 업로드 |
| 3 | 예약 현황 | 무노스 – 예약현황(일별집계) | 파일 업로드 |
| 4 | 날씨 현황 (부산 기장군) | 기상청 공공데이터 단기예보 API | 실시간 자동 호출 |
| 5 | 그린피 단가 현황 (월별/연도별 + 익월 확인) + 전자결재(승인) | 현장 수기 입력 (그린피 안내문 기준, 1단계는 회원 요금만) | 수기 입력 (월 1회) → 대표님 웹 승인 |
| 6 | 그린피 조정 시뮬레이션 (단가 조정 → 손익 영향 What-if, 부 단위) | 5번 + 일일매출/예약 실적 데이터 조합 | 화면에서 직접 입력 |

---

## 2. 실제 엑셀 샘플 분석 결과 (`/EXCEL` 폴더 3종)

화면 설계 전에, 실제 현장 다운로드 파일 3건을 열어 구조를 확인했다. **이 분석 결과가 이번 PRD에서 가장 중요한 부분이며, 아래 3~4번 리스크는 반드시 현장 확인 후 착수해야 한다.**

### 2.1 예약현황(일별집계).xls — ✅ 파싱 난이도 낮음
- 시트 1개, 3개 헤더행(제목/필터조건/컬럼명) 이후 **1행 = 1일**로 8월 1일~31일 31행이 빈틈없이 존재.
- 컬럼: `일자, 요일, 전체타임, 1부/2부/3부 예약수, 예약합계, 1부/2부/3부 잔여수, 잔여합계, 인터넷/모바일/전화/기타 예약수, 09:00 회원수, 17:00 회원수, 회원증가수`
- `일자`는 `"20260801"` 형태의 **텍스트**(진짜 날짜 타입 아님) → `YYYYMMDD` 문자열 파싱 필요.
- 요청하신 "예약 현황" 기능과 구조가 정확히 맞아떨어짐. 소계/합계행 없음, 병합 셀 없음 → **가장 쉬운 파일**.

### 2.2 일일영업집계.xls — ✅ 확인 완료 (사용자 확정 답변 반영)
- 샘플 파일은 조회 기간을 "월 전체"로 잡아 추출한 것이라 5개 표가 병합 셀 481개로 뒤엉킨 월간 스냅샷 형태였으나, **사용자 확인 결과 무노스에서 조회 기간을 "하루"로 지정해 일별 재추출이 가능**함 (§8 결정사항 1).
- 또한 **5개 서브테이블 전체가 아니라 "매출 집계(Sub-table B)" 한 줄만 필요**: `그린피, 카트료, 대여료, 식음매출, 상품매출, 기타매출` (+ 매출합계) — 하루치 값 6~7개 숫자만 추출하면 됨. 업장별/카드사별 세부 내역(Sub-table C/D/E)은 1단계 범위에서 제외.
- 따라서 실제 파싱 난이도는 처음 우려보다 훨씬 낮음: 매일 "하루" 필터로 재추출된 파일을 업로드받아, 고정 위치의 매출 카테고리 6개 셀 값(`v`, 0원은 서식상 빈칸으로 보일 수 있으니 반드시 값 기준으로 읽음)만 뽑아 그날 날짜에 매핑하면 됨.

### 2.3 자금일보_26년 8월.xlsx — ⚠️ 파싱 난이도 중~높음
- **영업일 1일 = 시트 1개** 구조 (0803, 0804, … 0831), 8월 21개 시트. 주말(토/일)은 시트 자체가 없음 → 자금일보는 은행 영업일 기준이라 **주말치 데이터가 원천적으로 존재하지 않음**. 월 시작 주말(8/1~8/2)도 없어 8/3 시작 잔액부터가 사실상 월초 기준.
- 시트마다 동일한 고정 템플릿(구분/전일/입금/대체입/대체출/출금/금일 표 + 입출금 상세내역 + 은행별 잔액 + 환율 + 검증용 스트립)으로 65개 병합 셀 반복.
- 셀 값은 대부분 **수식**(예: `=+E7+F7+G7-H7-I7`)이며 캐시된 계산값(`v`)만 읽으면 되고, 재계산은 불필요.
- **예외 케이스 발견**: `0828` 시트(정상 자금일보) 외에 **`8월 28일`이라는 완전히 다른 레이아웃의 보조 시트**가 하나 더 있음 (거래처별 대형 지출 내역 리스트, 그날 법인세 중간예납 2.4억원 등 큰 지출이 있었던 날이라 상세 내역을 별도로 남긴 것으로 추정). 이런 "정규 패턴에서 벗어난 시트"가 매달 랜덤하게 나타날 수 있으므로, 파서는 **시트명이 `MMDD` 형식이 아니거나 헤더 시그니처가 다르면 자동으로 건너뛰거나 별도 처리**하는 예외 규칙이 필요함.

### 2.4 요약 표

| 파일 | 샘플에서의 실제 단위 | 파싱 난이도 | 설계 영향 |
|---|---|---|---|
| 예약현황(일별집계) | 1행=1일 | 낮음 | 요청 기능과 일치 |
| 일일영업집계 | 1일 필터 재추출 시 1건=1일 (매출집계 6개 항목만 사용) | 낮음 (범위 축소 확정) | §8 결정사항 1 반영 완료 |
| 자금일보 | 1시트=1영업일(주말 없음) | 중~높음 | 주말은 공란 처리로 확정 (§8 결정사항 4) |

---

## 3. 제안 아키텍처 (1단계, 서버/DB 직접 구축 없음)

> 이 섹션은 **권장안**이며, 확정 전 사용자 확인이 필요합니다 (§8 참조).

### 3.1 왜 "완전 클라이언트 전용"은 불가능한가
"새로고침해도 안 사라짐" + "대표님과 현장 직원이 같은 데이터를 봄"이라는 두 요구사항은 브라우저 로컬 저장소(localStorage 등)만으로는 충족 불가 — 각자의 브라우저에 데이터가 갇히기 때문. 따라서 **직접 서버 코드를 작성/운영하지 않으면서도 공유 저장소를 제공하는 관리형 백엔드(BaaS)** 가 필요함.

### 3.2 확정 스택 (사용자 결정 완료)
- **프론트엔드**: Next.js (React) — Vercel에 정적/서버리스 배포
- **공유 저장소**: **Firebase Firestore**로 확정 (프로젝트 ID `stonegate-8f538`, 리전 `asia-northeast3` 서울). 이유: 설정이 가장 간단하고, 무료 티어(Spark)가 넉넉하며 이 규모 트래픽은 무료 한도 내에서 충분히 운영 가능, Next.js/Vercel과의 연동 예제가 많음
- **Cloud Storage(원본 엑셀 파일 보관)는 사용하지 않음** (2026-09-09 결정). Google이 Firebase Storage 활성화에 Blaze(종량제/카드 등록) 요금제를 요구하도록 정책을 변경함 — 원본 파일 보관은 PRD상 필수 요건이 아니므로(파싱된 결과만 Firestore에 저장하면 충분), Storage 자체를 도입하지 않고 Spark 무료 요금제만으로 운영
- **엑셀 파싱**: `xlsx` (SheetJS) 라이브러리 — 이번 분석에서 3개 파일 모두 정상 파싱 확인됨, 브라우저(클라이언트)에서 파싱 후 결과만 서버 API Route로 전송
- **날씨**: 기상청 공공데이터포털 단기예보 API — 서비스 인증키 필요, 클라이언트에 키 노출 방지를 위해 Next.js API Route(서버리스 함수)에서 호출 후 캐싱(발표주기 3시간 단위이므로 "실시간"이라기보다 "주기적 자동 갱신")
- **접근 제어 및 쓰기 경로**: 대표님 조회 화면은 로그인 없이 링크로 오픈, Firestore 읽기는 공개. 현장 직원 업로드 화면(`/upload`)은 **PIN**으로 보호하며, **모든 쓰기(업로드 파싱 결과, 그린피 제출/승인)는 클라이언트가 Firestore에 직접 쓰지 않고 Next.js API Route를 거쳐 Firebase Admin SDK(서비스 계정 키)로만 처리** — Firestore 보안 규칙은 기본적으로 쓰기를 전부 차단(`allow write: if false`)하고, Admin SDK는 규칙을 우회하므로 서버 쪽 PIN 검증이 곧 유일한 쓰기 관문이 됨 (2026-09-09 결정, 클라이언트 규칙 기반 PIN 검증보다 안전)

### 3.3 데이터 흐름
1. 현장 직원이 무노스/더존에서 엑셀 다운로드
2. 웹페이지에 업로드 (드래그앤드롭)
3. 브라우저(또는 서버리스 함수)에서 `xlsx`로 파싱 → 정해진 스키마로 구조화
4. Firestore/Supabase에 저장 (기존 동일 일자 데이터가 있으면 덮어쓰기)
5. 대표님이 접속 시 저장된 최신 데이터를 그래프/카드로 렌더링

---

## 4. 데이터 모델 초안 (컬렉션/테이블 단위, 상세 스키마는 착수 시 확정)

- `reservations` : 일자별 예약현황 (파일 1) — 전체 컬럼 저장
- `dailySales` : 일자별 매출집계 (파일 2) — `그린피, 카트료, 대여료, 식음매출, 상품매출, 기타매출, 매출합계` 6~7개 필드만 저장
- `cashFlow` : 영업일별 자금일보 요약 (파일 3) — 주말은 문서 자체를 생성하지 않음(화면에서 "데이터 없음"으로 표시)
- `greenFeeRates` : 연/월별 그린피 단가표 (수기입력, 구조는 §9.2 참조) — 부(1부/2부/3부) × 시간대 × 요일군 × 회원(1단계는 회원만) 조합별 금액 + 카트료/캐디피 정액 + 날짜별 예외(요금 오버라이드/휴장) 리스트
- `greenFeeApprovals` : 월별 그린피 단가 제출·승인 이력 (제출자, 제출일시, 승인 여부, 승인일시) — 종이 서명란(담당/팀장/대표이사)을 대체하는 전자결재 기록
- `weatherCache` : 기상청 API 응답 캐시 (자동 갱신)
- `uploadPin` : 환경변수(서버 전용)로 관리, Next.js API Route에서만 비교/검증 (클라이언트에 노출 안 됨)

---

## 5. 화면 설계는 이번 단계에서 진행하지 않음
요청하신 대로 화면 설계(UI/와이어프레임)는 아래 §8의 확인 사항이 정리된 이후 별도로 진행합니다.

---

## 6. 단계별 로드맵

- **Phase 1 (지금)**: 엑셀 업로드 기반 수동 파이프라인, BaaS 공유 저장소, 날씨 자동 연동, 그린피 수기입력 + 시뮬레이션
- **Phase 2 (개발 리소스 합류 후)**: 무노스/더존 API 직접 연동으로 업로드 과정 제거, 자체 서버/DB 전환 검토

---

## 7. 확정된 결정사항 (2026-09-08)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 일일 매출 데이터 | 무노스에서 "하루" 단위 재추출 가능 확인됨. `일일영업집계` 파일 중 **매출집계(그린피/카트료/대여료/식음매출/상품매출/기타매출) 6개 항목만** 사용, 나머지 서브테이블(그린피 단가별 팀수, 업장별, 카드사별)은 1단계 범위 제외 |
| 2 | 기술 스택 | **Firebase Firestore**로 확정 (Cloud Storage는 미사용 — §3.2 참고) |
| 3 | 업로드 보안 | **업로드 PIN 도입** 확정 |
| 4 | 주말 자금 현황 | **공란 처리** — "데이터 없음(주말)"로 명확히 표시, 최근 영업일 값으로 채우지 않음 |
| 5 | "웹 결제" 의미 | **전자결재(승인) 프로세스**로 확정. 실제 카드/계좌 결제 연동 아님 — 종이 서명란(담당/팀장/대표이사)을 웹 승인 버튼으로 대체 |
| 6 | 비회원 그린피 | 1단계 범위 제외, **회원 요금만 관리** |
| 7 | 그린피 시뮬레이션 입력 단위 | **부(1부/2부/3부) 단위로 단순화** — 시간대별 세분화 없이 부별 대표 단가 조정으로 손익 영향 계산 |

---

## 8. 남은 결정 필요 항목 (기본안 제안 — 이견 없으면 이 기본안으로 진행)

### 8.1 예외 시트/파일 대응 정책 (기본안: 자동 스킵 + 화면 경고)
`8월 28일`처럼 정규 패턴(`MMDD` 시트명, 표준 헤더)을 벗어난 시트/파일이 매달 랜덤하게 생길 수 있음.
**제안 기본값**: 파서가 인식 실패한 시트/데이터는 조용히 무시하지 않고, 업로드 결과 화면에 "N개 시트 중 M개 인식됨, 인식 안 된 시트: 8월 28일" 식으로 **경고를 표시**하여 현장 직원이 즉시 알아챌 수 있게 함. 별도 관리자 알림(이메일 등)은 1단계 범위 밖으로 제외.

### 8.2 그린피 시뮬레이션 계산 로직 (기본안: 부 단위, 최근 30일 실제 라운드 수 평균)
"단가 조정 → 손익 영향" What-if 계산 시 (§7 결정사항 7: 부 단위로 확정):
**제안 기본값**: 예상 라운드 수 = **부(1부/2부/3부)별 최근 30일(또는 이번 달) 실제 평균 라운드 수**를 자동 산출해 기본값으로 채우되, 화면에서 대표님/임원진이 이 예상 라운드 수 자체도 직접 덮어쓸 수 있게 함.
- 입력: 부별 그린피 조정값(예: "1부 +10,000원") → 출력: 예상 매출 변동 = 조정값 × 해당 부 예상 라운드 수, 합산해 전체 매출/손익 영향 표시
- 예약현황(File 1) 데이터가 이미 `1부/2부/3부 예약수`를 보유하고 있어 부별 라운드 수 집계에 별도 데이터 추가 불필요

이견 없으시면 위 기본안대로 다음 단계인 **데이터 스키마 최종 확정 → 화면 설계(와이어프레임)** 로 진행하겠습니다.

---

## 9. UI 참고자료 (화면 설계 단계에서 활용 예정)

### 9.1 예약 현황 — 무노스 실제 화면 캡처 (2026-09-08 제공)
현장에서 예약 현황을 확인할 때 사용하는 무노스 화면은 **월간 캘린더 뷰**:
- 셀 하나 = 하루. 표시 값: `예약수/전체타임` (예: `84/126`) + **가동률 %** (예: `67%`)
- 요일 헤더 색상 구분 (SUN=빨강, SAT=파랑, 평일=검정), 오늘 날짜는 초록색 강조
- 전체타임(분모)이 84/88/126처럼 요일·시즌에 따라 달라짐 (2.1절 분석과 일치 — 유지보수/단축 운영일 반영)

**화면 설계 시 반영 방향**: 대표님 대시보드의 "예약 현황" 섹션도 이 캘린더 히트맵 형태를 기본 UI로 채택 (가동률 % 기준 색상 그라데이션 등 시각적 개선은 화면 설계 단계에서 구체화). 파일 1(`예약현황(일별집계)`)의 `예약합계`/`전체타임` 컬럼으로 동일한 뷰 재현 가능 — 별도 데이터 추가 수집 불필요.

### 9.2 그린피 안내문 — 실제 요금표 캡처 (2026-09-08 제공, 9월 기준)

현장에서 매달 대표님께 종이로 결재받는 "OO월 그린피 안내" 문서 구조 (문서 우상단에 담당/팀장/대표이사 서명란 존재 — 이를 §7 결정사항 5의 전자결재로 대체 예정):

| 구분 | 시간대 | 주중(월~금) | 주말(토) | 주말(일,공휴일) |
|---|---|---|---|---|
| 1부 | 첫팀~06:22 | 130,000원 | 160,000원 | 160,000원 |
| 1부 | 06:30~06:52 | 140,000원 | 170,000원 | 170,000원 |
| 1부 | 07:00~막팀 | 150,000원 | 180,000원 | 180,000원 |
| 2부 | 전타임 | 180,000원 | 200,000원 | 190,000원 |

| 구분 | 시간대 | (월~목) | (금,토) | (일,공휴일) |
|---|---|---|---|---|
| 3부 | 16:15~17:39 | 130,000원 | 150,000원 | 140,000원 |
| 3부 | 17:46~18:35 | 120,000원 | 140,000원 | 130,000원 |

**주의**: 1부/2부는 요일군이 `주중(월~금)/토/일·공휴일` 3분류인데, **3부만 요일군이 다름** (`월~목/금,토/일,공휴일`) — 즉 금요일이 3부에서만 "주말" 취급됨. 그린피 단가 입력 화면은 부(1/2/3부)마다 요일군 매핑이 다를 수 있음을 감안해 설계해야 함.

**기타 확정 정보**:
- 카트료: 팀당 100,000원 (정액, 세션/요일 무관) — 매출집계의 "카트료" 항목과 직접 매핑됨
- 캐디피: 1/2/3부 공통 150,000원 (정액)
- 정상 18홀 라운드 종료 기준 그린피
- **날짜별 예외 존재**: 예) "9/24(목) 주중요금 적용", "9/25(금) 추석당일 휴장" — 특정 공휴일/명절에 요일군과 다른 요금이 적용되거나 휴장되는 경우가 매달 있음 → `greenFeeRates`에 월 단위 정규 요금표 외에 **날짜별 예외 리스트**(요금 오버라이드 or 휴장 플래그)를 별도로 함께 입력받아야 함
- 1단계는 회원 요금만 관리 (§7 결정사항 6), 비회원 요금은 범위 제외
