# 진행 상황 요약 (Continuation Notes)

최종 업데이트: 2026-09-08
대상: 골프장 현황 대시보드 (`(주)오션디앤씨 스톤게이트CC`, 부산 기장군)

이 문서는 다른 세션/환경에서 작업을 이어갈 때 맥락을 빠르게 따라잡기 위한 요약입니다. 상세 요구사항과 결정 근거는 `PRD.md`를 참고하세요.

---

## 1. 지금까지 완료된 것

1. **PRD.md 작성 완료** — 배경, 목표, KPI, 핵심 기능 6종, 실제 엑셀 샘플 3종 분석 결과, 확정된 아키텍처/기술 결정 사항, 남은 미해결 항목까지 정리됨.
2. **화면 설계(와이어프레임) 완료 — 클릭 가능한 프로토타입으로 게시함**
   - 게시 링크: https://claude.ai/code/artifact/7e2ca611-c4aa-4a23-be07-370b9fd9a2c9
   - Claude Design 캔버스 프리뷰 기능으로 만든 3개 아트보드:
     - `design/Main.dc.html` — 대표님 데스크톱 대시보드 (자금/매출/예약캘린더/날씨/그린피단가/그린피 시뮬레이션 — 시뮬레이션은 실제 계산 동작)
     - `design/MainMobile.dc.html` — 같은 내용의 모바일 버전
     - `design/Upload.dc.html` — 현장 업로드 화면 (PIN 게이트, 테스트 PIN: `1234`, 엑셀 업로드 시뮬레이션, 그린피 입력 폼, 전자결재 상태)
   - `design/canvas.json` — 캔버스 레이아웃 매니페스트
   - `design/golf-dashboard-design.html` — 위 3개를 합쳐 실제 게시한 산출물 (재생성 가능한 빌드 결과물이라 `.gitignore` 처리됨, 저장소에는 없음)
3. **1차 코드 리뷰로 버그 2건 수정 완료**: (a) Upload 화면에서 `disabled="{{ }}"` 바인딩이 HTML 특성상 항상 잠기던 문제 → 클래스 기반 방식으로 수정, (b) 예약 캘린더 9/25이 그린피 안내문의 "추석 휴장"과 모순되던 것 → "휴장"으로 통일, (c) 모바일 화면에서 1부·2부/3부 요금표가 잘못된 요일군으로 합쳐져 있던 것 → 분리.

---

## 2. 확정된 핵심 결정 사항 (PRD.md §7 참조)

| 항목 | 결정 |
|---|---|
| 일일 매출 데이터 | 무노스에서 "하루" 단위 재추출 가능. `일일영업집계` 중 매출집계 6개 항목만 사용(그린피/카트료/대여료/식음매출/상품매출/기타매출) |
| 기술 스택 | **Firebase (Firestore + Cloud Storage)** — 서버 직접 구축 없이 공유 저장소 |
| 프론트엔드 | Next.js (Vercel 배포) |
| 업로드 보안 | PIN 도입 |
| 주말 자금 현황 | 공란("데이터 없음") 처리 |
| 그린피 "결제" 의미 | 실제 결제 아님, **전자결재(승인) 프로세스** — 종이 서명란 대체 |
| 비회원 그린피 | 1단계 범위 제외, 회원 요금만 관리 |
| 그린피 시뮬레이션 단위 | 부(1부/2부/3부) 단위로 단순화, 예상 라운드 수는 최근 30일 평균 기본값(수정 가능) |

---

## 3. 아직 안 끝난 것 / 다음에 이어서 할 것

### 3.1 미해결 — 답변 도중 끊긴 질문
**"현장 업로드 페이지 경로를 어떻게 할까?"** 질문이 사용자 응답 전에 중단됨.
- 옵션 A: `/upload` 같은 단순 경로 (PIN이 실질적 보호장치이므로 충분하다는 입장)
- 옵션 B: `/upload-sg2026` 처럼 추측 어려운 경로로 이중 방어
- **다음 세션에서 이 질문부터 사용자에게 다시 확인 필요**

### 3.2 §8 남은 결정 항목 (PRD.md 참고, 기본안 제시됨— 이견 없으면 그대로 진행)
- 예외 시트/파일 대응 정책 (기본안: 자동 스킵 + 화면 경고)
- 그린피 시뮬레이션 계산 로직 세부 (기본안: 최근 30일 실제 라운드 수 평균)

### 3.3 다음 큰 단계
화면 설계가 승인되면 → **실제 Next.js + Firebase 구현 착수**
- Firestore 컬렉션 설계는 PRD.md §4 초안 참고 (`reservations`, `dailySales`, `cashFlow`, `greenFeeRates`, `greenFeeApprovals`, `weatherCache`, `uploadPin`)
- 엑셀 파싱은 `xlsx`(SheetJS) 라이브러리로 이미 3개 실제 샘플 파일 파싱 검증 완료 (구조는 PRD.md §2 참고)
- 기상청 단기예보 API 연동 필요 (서비스 인증키 발급 필요 — 아직 미발급/미확인)

---

## 4. 중요 주의사항

- **`/EXCEL` 폴더는 `.gitignore`로 제외되어 있고 GitHub에 푸시되지 않습니다.** 실제 은행계좌번호·거래처·지급액 등 민감한 실제 재무 데이터가 들어있기 때문입니다. 이 저장소(`github.com/inus209120-star/golf_dashboard`)는 **현재 퍼블릭(공개) 저장소**이므로, 앞으로도 실제 민감 데이터(계좌번호, 실 매출액 등)는 절대 커밋하지 말 것 — 필요하면 저장소를 비공개로 전환하거나 별도 비공개 저장소/로컬로 분리해서 다룰 것.
- 그린피 요금표(회원 단가)는 PRD.md에 실제 수치가 그대로 기록되어 있음 — 이는 고객 안내용 공개 정보라 민감정보 아님, 문제 없음.
- 화면 설계의 예약 현황/매출/자금 수치는 전부 **예시(fabricated) 데이터**이며, 그린피 요금표만 실제 9월 안내문 수치를 반영함.
- 로컬에만 있고 저장소에는 없는 것: `/EXCEL/*.xls, *.xlsx` (원본 샘플 파일 3종), `design/golf-dashboard-design.html` (재생성 가능한 빌드 결과물).
