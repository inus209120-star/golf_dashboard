# 진행 상황 요약 (Continuation Notes)

최종 업데이트: 2026-09-22 (자금현황에 "총 보유자금" 큰 숫자 추가)
대상: 골프장 현황 대시보드 (`(주)오션디앤씨 스톤게이트CC`, 부산 기장군)

이 문서는 다른 세션/환경(집 PC 등)에서 작업을 이어갈 때 맥락을 빠르게 따라잡기 위한 요약입니다. 상세 요구사항과 결정 근거는 `PRD.md`를 참고하세요.

---

## 1. 지금까지 완료된 것

1. **PRD.md 작성 완료** — 배경, 목표, KPI, 핵심 기능 6종, 실제 엑셀 샘플 3종 분석, 확정 결정사항 정리됨.
2. **화면 설계(프로토타입) — 진행 중, 방향은 잡힘**
   - 게시 링크: https://claude.ai/code/artifact/7e2ca611-c4aa-4a23-be07-370b9fd9a2c9
   - 아트보드 4개: `design/Start.dc.html`(인트로 화면, 실제 스톤게이트CC 홈페이지 사진 사용), `design/Main.dc.html`(대표 데스크톱 — **왼쪽 고정 사이드바 + Dashboard 통합 요약 화면 + 매출/예약/자금/그린피/날씨 5개 전용 상세 페이지** 구조로 최종 확정), `design/MainMobile.dc.html`(⚠️ 아직 맨 처음 버전 그대로 — Main.dc.html의 이후 변경사항들이 반영 안 됨, 나중에 동기화 필요), `design/Upload.dc.html`(PIN 보호 업로드 화면, PIN: `1234`)
   - 디자인 세부 사항(색상/톤 등)은 "나중에 다시 다듬기"로 보류 중 — 지금은 실제 구현(Firebase/Next.js)에 집중하는 단계
   - **미해결 이슈 → 유력한 원인 발견 (2026-09-14)**: "매출현황 클릭해도 안 넘어간다" 리포트, `design/MainMobile.dc.html` 재검토 결과 **애초에 모바일 아트보드엔 페이지 이동 개념 자체가 없었음** — 매출/예약/자금/그린피/날씨/시뮬레이션 카드가 전부 한 화면에 쭉 나열되는 구조(데스크톱만 카드 클릭 시 상세 페이지로 이동하는 로직이 있음). 사용자가 모바일에서 클릭했던 거라면 버그가 아니라 애초에 그 기능이 없었던 것으로 설명됨. 확정하려면 사용자가 어느 화면에서 클릭했는지 한 번 더 확인 필요하지만, 정황상 이게 원인일 가능성 높음 → 아래 모바일 재설계로 근본 해결 예정.
3. **Firebase 프로젝트 설정 완료 + Next.js 코드베이스 착수** (아래 §4, §5 참고) — Admin SDK로 Firestore 쓰기/읽기 왕복 테스트(`/api/health`)까지 성공 확인됨 (2026-09-10)

---

## 2. 확정된 핵심 결정 사항 (PRD.md §7 참조)

| 항목 | 결정 |
|---|---|
| 일일 매출 데이터 | 무노스 "하루" 단위 재추출 가능. 매출집계 6개 항목만 사용 |
| 기술 스택 | **Firebase (Firestore)** + Next.js (Vercel 배포) |
| Storage(원본 엑셀 파일 보관) | **사용 안 함** — Google이 Firebase Storage에 Blaze(결제) 요금제를 요구하도록 정책 변경, PRD상 원본 파일 보관은 필수 요건이 아니라 파싱된 데이터만 Firestore에 저장하는 것으로 결정 (2026-09-09) |
| 업로드 보안 | PIN 도입 + **쓰기 작업은 전부 서버(Next.js API Route)에서 Firebase Admin SDK로 처리** — 클라이언트가 Firestore에 직접 쓰지 않음 (Firestore 규칙은 읽기만 공개, 쓰기는 기본 차단) — 아키텍처 세부 결정 (2026-09-09) |
| 업로드 페이지 경로 | `/upload` 확정 |
| 주말 자금 현황 | 공란("데이터 없음") 처리 |
| 그린피 "결제" 의미 | 전자결재(승인) 프로세스, 실제 결제 아님 |
| 비회원 그린피 | 1단계 범위 제외 |
| 그린피 시뮬레이션 단위 | 부(1부/2부/3부) 단위, 최근 30일 평균 라운드 수 기본값 |
| Vercel 호스팅 플랜 | 우선 **Hobby(무료) 플랜**으로 배포 — Hobby는 원래 개인/비상업 용도 전용이라 엄밀히는 회사 내부 툴 용도상 Pro($20/월) 대상이지만, 소규모 내부 대시보드 트래픽 수준이라 당장은 Hobby로 가고 트래픽이 늘거나 정식 서비스화되면 그때 Pro로 전환하기로 결정 (2026-09-14) |

---

## 3. 남은 결정 항목 (PRD.md §8, 기본안 있음 — 이견 없으면 그대로 진행)
- 예외 시트/파일 대응 정책 (기본안: 자동 스킵 + 화면 경고)
- 그린피 시뮬레이션 계산 로직 세부 (기본안: 최근 30일 실제 라운드 수 평균 — 이미 프로토타입에 반영됨)

---

## 4. 다음에 이어서 할 것 (착수 순서)

### 4.1 Firebase 계정/키 세팅 — 완료
1. ✅ Firebase 프로젝트 생성 완료
2. ✅ Firestore Database 활성화 완료
3. ⏭️ Storage — 건너뛰기로 결정 (위 §2 참고)
4. ✅ 웹 앱 등록 + `firebaseConfig` 확보 완료
5. ✅ 서비스 계정 키(Admin SDK) 발급 완료 — 파일 `stonegate-8f538-firebase-adminsdk-fbsvc-689b1b064f.json`을 프로젝트 루트에서 읽어 `web/.env.local`에 옮겨 넣음 (`.gitignore`로 보호됨, 원본 json 파일도 계속 로컬에 남아있으나 커밋 대상 아님)

### 4.2 Next.js 코드베이스 — 착수함
- ✅ `web/` 폴더에 Next.js(App Router, TypeScript) 프로젝트 스캐폴딩 완료, `firebase` + `firebase-admin` + `xlsx` 설치 완료
- ✅ `web/src/lib/firebase-client.ts` (브라우저용, 읽기 전용 Firestore) / `web/src/lib/firebase-admin.ts` (서버 전용, Admin SDK) 작성 완료
- ✅ `web/src/app/api/health/route.ts` — Firestore 쓰기/읽기 왕복 테스트 성공 확인 (`{"ok":true,...}`)
- ✅ `web/.env.local` 설정 완료 (Firebase client config + Admin SDK 자격증명 + `UPLOAD_PIN=1234` 임시값 — **실제 운영 전 반드시 변경할 것**)
- ✅ **Firestore 데이터 스키마 타입 정의 완료** — `web/src/types/firestore.ts` (`ReservationDoc`, `DailySalesDoc`, `CashFlowDoc`(은행별 배열 + 입출금 상세 포함), `GreenFeeRatesDoc`(1부/2부/3부, 3부만 요일군 다름 반영), `GreenFeeApprovalDoc`, `WeatherCacheDoc`, `UploadLogDoc`), 컬렉션 이름 상수는 `web/src/lib/collections.ts`
- ✅ **Firestore 보안 규칙 작성 완료** — `web/firestore.rules` (대시보드 데이터 컬렉션 전체 공개 읽기 / 쓰기 전부 차단). `web/firebase.json` + `web/.firebaserc`(project: `stonegate-8f538`)도 추가해둠.
  - ✅ **콘솔에 수동 게시 완료 + 클라이언트 SDK로 직접 검증 완료 (2026-09-11)**: `weatherCache` 등 허용된 컬렉션은 읽기 성공, `_health`는 읽기 거부(`permission-denied`), 클라이언트 직접 쓰기는 전부 거부 — 의도한 대로 정확히 동작 확인됨.
- ✅ **엑셀 파싱 로직 3종 작성 + 실제 샘플 파일로 검증 완료** (`web/src/lib/parsers/`)
  - `reservation.ts` — 예약현황(일별집계) .xls. 헤더 행("일자")을 텍스트로 찾아 위치를 특정, 18개 컬럼 그대로 매핑. 실제 샘플(8월 31일치) 100% 정확히 파싱 확인.
  - `cashFlow.ts` — 자금일보 .xlsx. 시트명이 `MMDD` 형식이 아니면 자동 스킵(§8.1 결정 그대로 구현, 실제로 "8월 28일" 이형 시트 정상 스킵 확인). 은행별 전일/입금/출금/금일 + 입출금 상세 항목까지 파싱. **실제 21일치 샘플 전부에서 "전일+입금-출금=금일" 검증 100% 통과.** 디버깅 중 실제 버그 2개 발견·수정: (1) 병합된 제목 행이 은행 데이터로 잘못 인식되던 것, (2) 회사 전체 합계에 계좌 간 내부이체(대체입/대체출)가 이중 반영되던 것 — 개별 은행별로는 내부이체를 반영해야 그 은행 자체의 잔액 계산이 맞고, 전사 합계에서는 내부이체가 서로 상쇄되므로 반영하면 안 됨(로직으로 확인 및 수정 완료).
  - `dailySales.ts` — 일일영업집계 .xls 중 "매출집계" 서브테이블만 파싱 (그린피/카트료/대여료/식음/상품/기타매출 6항목 + 합계). 파일 안에 적힌 "영업일자 : YYYY년MM월DD일 ~ YYYY년MM월DD일" 범위를 직접 읽어서, 시작일=종료일이면 그 날짜로 자동 확정하고 **범위가 하루보다 넓으면 저장을 거부하고 재추출을 요청** — 실제 "하루 단위" 샘플이 아직 없는 리스크를 이 안전장치로 관리 (§4.3에서 실제 1일치 파일로 최종 재검증 필요, 그 전까지 이 가드가 잘못된 데이터 저장을 막아줌). 디버깅 중 실제 버그 발견·수정: "그린피"라는 헤더가 무관한 다른 서브테이블(그린피 단가별 팀수표)에도 존재해서 엉뚱한 값(0)을 읽어오던 것 — "카트료"(이 서브테이블에만 존재하는 유일 텍스트)를 기준점으로 삼아 가장 가까운 헤더를 선택하도록 수정.
- ✅ **`/upload` 페이지 + API Route 3종 실제 구현 완료, 실제 파일로 전체 파이프라인(파싱→PIN검증→Firestore저장→읽기 확인) 엔드투엔드 테스트 통과**
  - `web/src/app/upload/page.tsx` — PIN 게이트(클라이언트, UX용) → 파일 선택 시 브라우저에서 즉시 파싱 → `/api/upload/*`로 파싱된 JSON + PIN 전송. **디자인은 아직 안 입힘 (기본 HTML 그대로) — 나중에 프로토타입 디자인 적용 예정, 지금은 기능 검증 우선**
  - `web/src/lib/upload-auth.ts` + `web/src/app/api/upload/{reservation,cashflow,sales}/route.ts` — PIN을 서버에서 재검증(진짜 보안 경계, `UPLOAD_PIN` 환경변수와 비교) 후 Admin SDK로 Firestore 저장 + `uploadLog` 컬렉션에 기록
  - 실제 샘플 3개 파일로 로컬 서버에 직접 업로드 테스트: 예약현황 31/31 저장, 자금일보 21/21 저장("8월 28일" 이형 시트는 정상 스킵), 매출은 실제 파일이 월간 범위라 위 안전장치가 정상적으로 저장을 거부함(의도한 동작), 틀린 PIN은 401로 거부됨 — 전부 확인. Firestore에서 직접 읽어서 데이터도 재확인함.
- ✅ **대시보드 실제 구현 완료** (`web/src/app/page.tsx` + `web/src/app/DashboardClient.tsx`) — 프로토타입(design/Main.dc.html)의 레이아웃·CSS를 `web/src/app/globals.css`로 그대로 옮기고, 실제 Firestore 데이터에 연동함
  - 사이드바 + Dashboard 통합 요약 + 5개 상세 화면(매출/예약/자금/그린피/날씨) 전부 실데이터 연동
  - 예약 캘린더/그린피 시뮬레이션 등에서 쓰던 "가짜로 비율 나눈" 값들을 실제 저장된 필드(부별 예약수, 은행별 입출금 등)로 교체 — 프로토타입보다 오히려 더 정확해짐
  - 데이터가 아직 없는 항목(오늘 매출, 그린피 단가표, 날씨)은 "데이터 없음" 빈 상태로 정상 표시 확인
  - **실제 업로드된 8월 데이터로 화면 검증 완료**: 자금 잔액 "3.2억"(=321,907,935원, 8/31 기준) 정확히 일치, 예약 가동률 "56%"(=49/88, 8/31 기준) 정확히 일치
  - ⚠️ **중요 버그를 빌드 단계에서 발견·수정**: `next build` 결과 `/` 페이지가 정적(Static)으로 빌드되고 있었음 — 이대로 배포했으면 배포 시점 데이터가 고정되고 이후 업로드해도 화면이 안 바뀌는 심각한 문제였음. `export const dynamic = "force-dynamic"` 추가로 매 요청마다 새로 데이터를 가져오도록 수정, 재빌드로 확인 완료(`ƒ /` 동적으로 표시됨)
  - 타입체크(`tsc --noEmit`) + ESLint + 프로덕션 빌드(`next build`) 전부 통과
- ✅ **그린피 단가 입력 + 전자결재(승인) 흐름 실제 구현 완료**
  - `/upload` 페이지에 그린피 입력 폼 추가 (실제 9월 안내문 구조 그대로: 1부 3구간/2부 1구간 - 주중·토·일공휴일 3요일군, 3부 2구간 - 월~목·금토·일공휴일 다른 3요일군, 카트료·캐디피 정액, 날짜별 예외 추가/삭제) — 대상 월(`YYYY-MM`) 지정해서 제출하면 PIN 검증 후 `greenFeeRates/{yearMonth}`에 `status:'pending'`으로 저장 + `greenFeeApprovals`에 이력 기록
  - 대시보드 그린피현황 화면에 **"승인하기" 버튼** 추가 (`/api/greenfee/approve`) — 클릭하면 `status:'approved'`로 변경 + 승인 이력 추가 + 화면 새로고침(`router.refresh()`)
  - ⚠️ **설계 결정 사항**: 승인 API는 PIN 보호가 없음 — 대시보드 자체가 "링크 소지 = 대표님" 신뢰 모델(PRD 원래 설계)이라 업로드 PIN과는 별개로 취급함. 나중에 더 강한 보안이 필요하면 승인 전용 PIN 추가 고려 가능
  - 가짜 미래 월("2099-01")로 제출→승인 전체 흐름 테스트 완료: PIN 틀림(401), 정상 제출(pending), 승인(approved), 존재 안 하는 월 승인 시도(404) 전부 확인. 타입체크+lint+프로덕션 빌드 전부 통과
- ✅ **사용자가 제시한 대시보드 스펙(KPI카드/차트/테이블 구성) 검토 후 반영 완료**
  - 논의 결과: 업로드는 계속 PIN 분리 유지, 레이아웃은 지금 사이드바 구조 유지하되 요청 요소들을 각 페이지 안에 반영, "목표 대비 달성률"은 목표값 개념이 아직 없어서 제외하고 **KPI 카드 형태(스타일)만** 적용하기로 확정
  - **매출현황**: KPI 카드 3개(매출액/객단가·RevPAR = (그린피+식음+카트료)÷예약팀수/예약팀수+가동률 — 예약 데이터와 날짜 기준 조인해서 계산) + 월간 매출 추이 막대(데이터 쌓일수록 채워짐) + 최근 영업일 상세 테이블(매출+예약팀수+가동률 조인)
  - **예약현황**: 일별 예약수 vs 잔여수 스택형 막대그래프(전체타임 대비) + 예약 채널별 비중 도넛(월 집계, 인터넷/모바일/전화/기타)
  - **그린피현황**: 연도별 조회 UI 추가(연도 선택 + 12개월 그리드, 데이터 있는 달은 녹색 표시) — `greenFeeRates` 전체를 서버에서 가져오도록 `page.tsx` 변경, `reservations` 전체 이력도 함께 전달해서 매출-예약 조인에 사용
  - 개발 중 실제 런타임 버그 1건 발견·수정: `reservationByDate`를 사용하는 코드보다 아래쪽에 선언해서 "Cannot access before initialization" 500 에러 발생 → 선언 위치를 위로 옮겨서 해결 (타입체크는 통과했지만 런타임에서만 걸러진 문제라, 실제 서버 켜서 확인하는 게 왜 중요한지 다시 확인됨)
  - 타입체크+lint+프로덕션 빌드+실제 서버 재확인 전부 통과 (자금 3.2억/가동률 56% 등 기존 실데이터도 여전히 정확)
- ✅ **기상청(KMA) 날씨 API 연동 완료** (`weatherCache/current` 채우기)
  - data.go.kr이 아니라 **기상청 자체 API허브(apihub.kma.go.kr)**의 단기예보 조회서비스(`VilageFcstInfoService_2.0/getVilageFcst`) 사용 — 인증 방식이 짧은 `authKey` 파라미터 하나(기존 data.go.kr의 긴 `serviceKey`와는 다른 별도 시스템). 사용자가 apihub.kma.go.kr에서 직접 발급받은 실제 `authKey` 적용, 웹서치로 공식 예시 키들과 길이·형식 대조해 정상 키임을 확인함.
  - 부산 기장군 기장읍 격자좌표는 표를 참고하지 않고 **LCC-DFS 변환 공식으로 직접 계산**(`nx=100, ny=77`) — 기장역 실좌표(35.2429/129.2185) 기준으로 검증.
  - `web/src/lib/weather.ts`: `fetchAndCacheWeather()` — 발표주기(02/05/08/11/14/17/20/23시, 발표 후 10분 대기) 반영해 가장 최근 `base_date`/`base_time` 계산 → API 호출 → 카테고리별(TMP/POP/SKY/PTY/TMN/TMX) 값을 슬롯(날짜+시간)별로 그룹핑 → 가장 가까운 슬롯을 "현재" 스냅샷으로, 날짜별 최저/최고·최대 강수확률을 모아 5일 예보 배열로 가공 → `weatherCache/current`에 저장
  - `web/src/app/api/weather/refresh/route.ts` — 수동/온디맨드 갱신용 GET 엔드포인트 (나중에 Vercel Cron 대상으로 재사용 가능)
  - `web/src/app/page.tsx` — 대시보드 로드 시 캐시가 **3시간(KMA 재발표 주기) 이상 오래됐으면 자동으로 재조회**, API 실패 시에도 기존 캐시(또는 null)로 폴백해서 날씨 오류가 대시보드 전체를 죽이지 않도록 처리
  - **실제 서버로 라이브 테스트 완료**: `/api/weather/refresh` 호출 → 실제 KMA 응답 성공(`resultCode: "00"`), 파싱 결과 정상(예: 25℃/맑음/강수확률 0%, 5일 예보) → 대시보드 홈(`/`)에 실제 값 렌더링 확인 → 타입체크/lint/프로덕션 빌드(`/`는 여전히 동적 `ƒ`) 전부 통과
- ✅ **종합영업일보 기반 "실제 내장 팀수/인원" 연동 완료** (2026-09-16)
  - 배경: 사용자가 `EXCEL/` 폴더에 무노스 "종합영업일보" 9/1~9/13 일자별 파일 13개(하루 1파일)를 추가하고, 예약현황에 팀수와 별도로 인원도 함께 기록해달라고 요청
  - 이 리포트는 기존에 파싱해온 "일일영업집계"/"예약현황(일별집계)"와는 전혀 다른 새 리포트(같은 무노스 시스템, 다른 서식) — 매출(입장료/카트료/프로샵/식음료/기타)·결제수단별 집계·향후 10일 예약 파이프라인·"팀수 및 객단가"(당일/당월/당년 실제 내장 팀수·인원·팀당인원·객단가) 등 여러 하위 표가 한 시트에 들어있음. **이번엔 그중 "팀수 및 객단가" 표의 "당 일" 행(실제 내장 팀수/인원/팀당인원)만 반영** — 나머지(매출·결제수단·예약파이프라인)는 범위 밖으로 명시적으로 남겨둠(매출 카테고리가 기존 `dailySales` 스키마(그린피/카트료/대여료/식음/상품/기타)와 1:1로 안 맞아서 매핑을 임의로 정하지 않음 — 필요해지면 별도로 논의).
  - **왜 기존 `reservations` 컬렉션에 합치지 않았는지**: `reservations/{date}`는 예약 파이프라인(1/2/3부 예약수, 채널별, 잔여수 등) 스키마라 이 새 리포트가 주는 "실제 내장 팀수/인원"과 의미가 다르고, 9월 1~13일은 예약현황(일별집계) 파일이 아직 없어서 병합 저장하면 나머지 필수 필드가 비어 대시보드의 기존 계산(가동률/캘린더/월별 집계 등)이 깨질 위험이 있었음 → 독립된 `dailyVisitors/{date}` 컬렉션으로 신설(날짜/요일/팀수/인원/팀당인원)
  - `web/src/lib/parsers/dailyVisitors.ts` — 고정 행 위치에 의존하지 않고 "구분" 옆에 "팀수", 그 옆에 "인원"이 있는 행을 찾아 헤더로 특정(시트 안에 "영업일수" 표도 "구분" 헤더를 쓰기 때문에 인접 텍스트로 구분), 그 아래 "당 일"(공백 포함 원문 그대로) 행을 데이터로 읽음. 리포트가 원래 "하루 단위"라 `dailySales`처럼 범위 검증이 필요 없음.
  - `web/src/app/api/upload/daily-visitors/route.ts` — PIN 검증 후 배치 쓰기(`reservations` 업로드와 동일 패턴)
  - `web/src/app/upload/page.tsx` — 하루 1파일이라 여러 날짜 파일을 한 번에 선택(`multiple`)해서 올리는 업로더 추가
  - `web/src/app/page.tsx` / `DashboardClient.tsx` — `dailyVisitors` 조회·전달, 예약현황 화면에 "실제 내장 현황(팀수/인원)" 표 추가 + 날짜 선택 상세 패널에 "실제 내장 인원" 스탯 추가(예약 데이터가 있는 날짜에 한해)
  - **검증**: 13개 파일 전부 정상 파싱 확인(예: 9/1 팀수 57·인원 216·팀당인원 3.79) — 교차검증으로, 9/1 파일 안의 "향후 예약" 표에 적힌 9/2일 총팀수(66)가 9/2 파일 자체의 "당일" 팀수(66)와 정확히 일치함을 확인해 파싱 로직이 올바른 행을 읽고 있음을 재확인. 실제 로컬 서버로 틀린 PIN(401 거부)·정상 업로드(13/13 저장) 테스트 통과, 대시보드 홈 렌더링 결과에 13개 문서 데이터가 정상 전달됨을 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **매출 데이터 소스를 일일영업집계 → 종합영업일보로 완전 전환** (2026-09-16)
  - 배경: 직원들이 앞으로 매출 업로드를 무노스 "일일영업집계" 대신 "종합영업일보"로 대체하겠다고 확인 → 사용자와 논의 후 **완전 전환**으로 확정(§4.2 위쪽에서 만든 종합영업일보 파서를 매출에도 그대로 활용하는 김에, 예전 일일영업집계 경로는 걷어냄)
  - **스키마 변경**: `DailySalesDoc`에서 `rentalFee`(대여료) 필드 삭제 — 종합영업일보엔 대여료가 별도 항목으로 없고, 금액이 크지 않다는 사용자 판단으로 그냥 없는 걸로 확정(다른 항목에 억지로 합치지 않음). 나머지 필드도 종합영업일보 표기를 그대로 따름: `greenFee`=입장료, `cartFee`=카트료, `foodBeverage`=식음료, `proShop`=프로샵, `other`=기타, `total`=총매출.
  - **삭제**: `web/src/lib/parsers/dailySales.ts`(일일영업집계 파서, 새 스키마와 안 맞아 제거), `web/src/app/api/upload/sales/route.ts`(단건 업로드 라우트)
  - **신규**: `web/src/lib/parsers/businessDailySales.ts` — 종합영업일보의 "영업현황" 매출 표("매출" 표식 옆에 입장료+카트료가 같은 행에 있는 걸로 헤더 특정, 그 아래 "일계" 행을 읽음) 파싱. `web/src/app/api/upload/daily-sales/route.ts` — `daily-visitors`와 동일한 배치 저장 패턴(여러 날짜 파일을 한 번에)
  - `/upload` 페이지의 매출 업로더와 팀수/인원 업로더를 **하나로 통합**(`BusinessDailyUploader`) — 종합영업일보 파일 하나에서 매출·인원 둘 다 뽑아 각자의 컬렉션(`dailySales`, `dailyVisitors`)에 동시 저장. 이제 이 리포트 파일을 한 번만 올리면 됨.
  - **검증**: 실제 13개 파일(9/1~9/13) 전부 매출 파싱 성공 + **카테고리 합계가 총매출과 정확히 일치**(13일 전부 반올림 오차 없이 검증 통과) → 로컬 서버에 실제 배치 업로드(13/13 저장) → 대시보드 홈 렌더링에 실제 9월 매출 수치(예: 9/1 그린피 30,240,000원, 총매출 41,644,000원) 정상 반영 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **실제 앱(사이드바→모바일 내비게이션)에 모바일 최적화 1차 적용 완료** (2026-09-16)
  - 배경: 사용자가 "직원/대표님 모두 모바일로 많이 볼 것"이라며 실제 모바일 최적화를 요청. 지난 집 PC 세션의 모바일 요약 화면 시안(§7.2, 하단 탭바 컨셉)을 실제 코드에 반영.
  - **하단 탭바 내비게이션 추가** (`web/src/app/DashboardClient.tsx` — 사이드바와 같은 6개 항목, 기존 `SideIcon` 아이콘 재사용) — 860px 이하에서 사이드바(`display:none`)를 완전히 대체. 이전엔 좁은 화면에서 사이드바가 아이콘만 남은 가로 스크롤 줄로 바뀌는 임시방편이었는데, 그게 바로 예전 "매출현황 클릭해도 안 넘어간다" 버그 리포트의 유력 원인(§1)이었을 가능성이 있어 이번에 근본적으로 정리함.
  - **실제 오버플로우 버그 2건을 헤드리스 브라우저로 직접 렌더링해서 발견·수정** (아래 검증 방법 참고, 타입체크/코드 리뷰만으로는 못 잡는 종류):
    1. 예약현황 캘린더(`.cal-grid`/`.dow-row`, 7열)와 그린피 월 선택 그리드(`.month-grid`, 6열)·날씨 5일 예보(`.forecast-row`)가 `grid-template-columns: repeat(N, 1fr)`로만 되어 있어서, 각 셀의 텍스트 콘텐츠(예: "49/126팀")가 1fr이 허용하는 폭보다 넓으면 그리드가 강제로 넓어져 모바일에서 192px나 옆으로 밀려나가던 문제 — `minmax(0, 1fr)`로 바꿔서 실제로 폭에 맞게 줄어들도록 수정 (CSS 그리드의 잘 알려진 함정: `1fr`은 트랙이 콘텐츠의 min-content보다 작아지는 걸 막음).
    2. Dashboard 요약 화면의 날씨 미니카드가 인라인 `style={{gridColumn:"span 2"}}`로 2칸을 차지하도록 되어 있었는데, 모바일에서 그리드 자체가 1열로 바뀌어도 이 span이 그대로 남아 브라우저가 암시적(implicit) 2번째 트랙을 자동 생성 — 결과적으로 미니카드들이 모바일에서도 2열로 남아있던 원인. `.mini-card-wide` CSS 클래스로 교체하고 모바일 미디어쿼리에서 `grid-column:auto`로 되돌리도록 수정.
  - 은행별 자금흐름/그린피 요금표 등 열이 많은 테이블 5곳을 `.table-scroll`(overflow-x:auto) 래퍼로 감싸서, 화면이 좁아도 페이지 전체가 밀리는 대신 표 안에서만 가로 스크롤되도록 처리. 그 외 카드 패딩 축소, 은행 목록 열 폭 조정, 검색창(미구현 장식 요소) 모바일에서 숨김 등 자잘한 여백/오버플로우 정리.
  - **검증 방법**: 로컬에 `playwright-core`(devDependency로 설치, 브라우저 재다운로드 없이 시스템 설치된 Edge를 `channel:'msedge'`로 구동)로 헤드리스 브라우저를 띄워 375×812(아이폰 크기) 뷰포트에서 대시보드 5개 화면(요약/매출/예약/자금/그린피) + 날씨 화면 + 예약 상세 패널까지 전부 실제 스크린샷 촬영, 각 화면의 `scrollWidth vs clientWidth`를 스크립트로 비교해 가로 오버플로우 여부를 자동 확인. 최초 실행에서 요약 화면(14px)과 예약현황 화면(192px) 오버플로우를 실제로 잡아냈고, 위 수정 후 전 화면 오버플로우 0으로 확인. 콘솔 에러 없음도 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
  - **참고**: 데스크톱 KPI 5칸 시안(§7.1)과 모바일 요약 화면의 KPI-카드 스타일 자체(아이콘+칩 형태)는 이번엔 반영 안 함 — 이번 범위는 "내비게이션 + 오버플로우 없이 잘 보이게" 까지로 한정, 시각 디자인 변경은 별도 논의 필요
- ✅ **전체 서체를 Pretendard로 교체** (2026-09-16)
  - 사용자가 다른(무관한) 프로젝트 스크린샷을 보여주며 그 서체로 바꿔달라고 요청 → 서체 모양상 Pretendard로 추정, 확인 후 적용
  - `pretendard` npm 패키지 설치, variable 폰트(`pretendardvariable.css`, woff2 1개 파일) 방식으로 `web/src/app/layout.tsx`에서 로드. 기존에 있었지만 실제로는 어디에도 쓰이지 않던(죽은 설정) Next.js 기본 Geist 폰트 설정은 제거.
  - `globals.css`의 `body` font-family를 `"Pretendard Variable", Pretendard, system-ui, ...`로 변경 (버튼/카드 등은 대부분 `font-family: inherit`라 자동으로 같이 바뀜). `/upload` 페이지(PIN 게이트+업로드 폼)는 원래부터 "디자인 미적용, 기능 검증 우선"으로 남겨둔 상태라 이번에도 그대로 둠(스타일 적용 안 함).
  - 겸사겸사 `<html lang="en">`으로 남아있던 것도 `lang="ko"`로 수정(실제 콘텐츠는 전부 한글이라 원래 잘못된 값이었음)
  - **검증**: 헤드리스 브라우저로 `getComputedStyle(document.body).fontFamily`가 실제로 `"Pretendard Variable", Pretendard, ...`로 적용됨을 확인, 데스크톱/모바일 스크린샷으로 실제 글자 모양이 바뀐 것도 육안 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **날씨 현황에 풍속/풍향/습도 추가** (2026-09-16)
  - 사용자가 날씨 상세 화면 아래쪽이 비어 보인다며 바람/해풍 등 추가 데이터 요청 → 기상청 단기예보 API(`getVilageFcst`) 응답에 이미 WSD(풍속)/VEC(풍향)/REH(습도) 카테고리가 포함돼 있음을 실제 응답으로 재확인, 새 API 연동 없이 기존 파서만 확장
  - `web/src/types/firestore.ts` — `WeatherCacheDoc`에 `windSpeed`(m/s), `windDir`(16방위 한글, 예: "북동풍"), `humidity`(%) 필드 추가
  - `web/src/lib/weather.ts` — `firstSlot`에서 WSD/VEC/REH 파싱, VEC(0~360도)를 16방위 한글 라벨로 변환하는 `windDirLabel()` 추가
  - `web/src/app/DashboardClient.tsx` — 날씨 현황 화면의 예보 카드 아래에 "현재 기상 상세"(풍속/풍향/습도 3칸) 섹션 추가
  - **검증**: `/api/weather/refresh` 실제 호출로 풍속 6.3m/s·북동풍·습도 55% 등 실제 값 확인, 모바일 스크린샷으로 레이아웃도 확인(오버플로우 없음). 타입체크+lint+프로덕션 빌드 전부 통과.
  - 참고: 같은 API가 WAV(파고)도 제공하지만 골프장엔 직접 관련이 낮아 보류함 — 필요시 언제든 같은 방식으로 추가 가능
- ✅ **기상특보(강풍/호우/폭염/한파/태풍 등) 연동 완료** (2026-09-16)
  - 배경: "낙뢰/기상특보 데이터도 넣을 수 있나" 질문에서 시작 — 조사 결과 **진짜 낙뢰(번개) 관측은 레이더/이미지 기반의 완전히 별도 API**라 이번 범위에서 제외하고, 강풍이 낙뢰를 동반한 뇌우와 자주 같이 뜨는 점을 감안해 **일반 기상특보 11종(강풍/호우/대설/건조/폭풍해일/한파/태풍/황사/폭염/열대야/풍랑)**으로 방향을 잡음 (사용자 확인 완료)
  - **API 조사 과정에서 발견한 것들** (전부 실제 호출로 검증):
    - apihub.kma.go.kr는 API마다 **개별 활용신청이 필요** — 이번에도 지난 기상청 온보딩처럼 사용자가 콘솔에서 신청 진행 (특보자료 API, 특보현황 조회 API 총 2건 신청)
    - 이 API 계열(`typ01/url/*.php`)은 단기예보 API(JSON/UTF-8)와 달리 **옛날 방식 텍스트 응답 + EUC-KR 인코딩** — Node의 `TextDecoder("euc-kr")`로 정상 디코딩 확인
    - 부산 기장군의 특보구역코드는 `L1082500`(부산동부) — 기상청 공식 페이지(weather.go.kr 특보구역 안내)에서 "기장군·해운대구·수영구·남구"가 부산동부에 속함을 직접 확인, 특보구역 코드 CSV로 교차 확인. 부산 전역 단위로 발표되는 특보(폭염/한파/황사 등) 대비 `L1150000`(부산 전체)도 함께 감시 대상에 포함
    - 최종적으로 쓴 API는 `wrn_now_data.php`(특보현황 조회, "현재 발효 중"만 반환, 지역코드로 필터 안 되어 전국 목록을 받아 REG_ID로 직접 필터링) — 특보종류/수준이 이미 한글 텍스트로 옴("강풍", "주의보" 등)이라 별도 코드 매핑 불필요
  - `web/src/types/firestore.ts` — `WeatherWarningItem`(type/level/regionName/effectiveFrom/effectiveTo) 추가, `WeatherCacheDoc.warnings` 필드 추가
  - `web/src/lib/weather.ts` — `fetchActiveWarnings()` 추가, 날씨 갱신 시 같이 호출(실패해도 기존 기온/예보 데이터는 살리는 방어적 처리, 기존 패턴과 동일)
  - `web/src/app/DashboardClient.tsx` — 날씨 현황 화면 최상단에 빨간 경고 배너(특보구역/종류/수준/발표시각), Dashboard 요약 화면의 날씨 타일에도 작은 배지로 표시
  - **검증**: 실제 API로 EUC-KR 디코딩 확인, 실시간 호출로 `warnings:[]`(현재 부산에 특보 없음, 정상) 확인, 배너/배지 렌더링은 Firestore에 임시 테스트 데이터를 넣어 스크린샷으로 실제 확인 후 즉시 재갱신으로 복구. 모바일 오버플로우 없음 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **Vercel 배포 완료** (2026-09-16)
  - 사용자가 GitHub 계정으로 Vercel 가입 → 저장소(`golf_dashboard`) Import (Root Directory를 반드시 `web`으로 지정 — 앱이 저장소 루트가 아니라 `web/` 서브폴더에 있어서) → `web/.env.local` 내용을 Vercel "Import .env" 기능으로 한 번에 가져와 환경변수 등록 → 배포
  - 첫 배포는 `FIREBASE_ADMIN_PRIVATE_KEY` 등 환경변수 미등록으로 빌드 실패했으나(예상된 실패), 환경변수 등록 후 재배포로 정상 성공
  - **실제 서비스 URL**: **https://golf-dashboard-vnzh.vercel.app** — 대표님은 이제 로그인 없이 이 링크로 바로 대시보드 접속 가능 (PRD 원래 목표)
  - 배포 직후 실제 URL로 직접 검증: `/api/health`(Firestore 왕복 정상), 홈페이지에 실제 데이터(매출 6,958만/자금 3.2억/가동률 56%) 로컬과 동일하게 정확히 렌더링 확인, `/upload` 페이지도 200 정상
  - 참고: `web/src/lib/firebase-admin.ts`가 이미 `privateKey?.replace(/\\n/g, "\n")` 처리를 해둔 덕분에, Vercel 환경변수 입력 시 개행이 `\n`(리터럴 백슬래시-n)으로 들어가도 정상 파싱됨 — 이런 흔한 배포 함정을 미리 피해감
- ✅ **모바일 실사용 버그 수정 + Dashboard 요약 화면 정리** (2026-09-17, 실제 배포 후 사용자가 자기 폰으로 써보면서 발견한 것들 위주)
  - **실제 폰 스크린샷으로 발견한 레이아웃 버그 2건**: (1) `.app-shell`이 모바일에서 세로 배치(column)로 바뀌는데 데스크톱용 `align-items: flex-start`가 그대로 남아있어서, 375px보다 넓은 실제 폰 폭(390~428px)에서 콘텐츠가 폭 전체로 안 늘어나고 왼쪽으로 쏠리며 오른쪽에 빈 공간이 생기던 문제 — `align-items: stretch`로 수정. (2) `viewport-fit=cover` 메타 태그가 아예 없어서 iOS가 안전영역(홈 인디케이터) 여백을 0으로 처리 — 하단 탭바 글씨가 홈 인디케이터랑 겹쳐 보이던 원인. `layout.tsx`에 `viewport` export 추가로 해결. 둘 다 헤드리스 브라우저 테스트(375px)에서는 안 잡히던 버그라 실제 기기 확인이 왜 중요한지 다시 확인됨.
  - **하단 탭바**: "요약" 항목 제거(매출/예약/자금/그린피/날씨 5개로 축소, 항목 줄어든 만큼 글씨(9.5→11px)·아이콘(17→20px) 확대), 각 아이콘에 고유 색 배지 추가(매출=파랑/예약=초록/자금=네이비/그린피=진초록/날씨=주황, 비활성 시 반투명)
  - **Dashboard 요약 화면 정리**: 상단 제목 "Dashboard"→"스톤게이트CC", 안내 문구 삭제. 사용자가 "위쪽 4칸 KPI(매출/가동률/잔액/날씨)랑 바로 아래 미니카드가 같은 숫자 중복 아니냐"고 지적 → 확인해보니 매출·자금·날씨 미니카드는 이미 자체적으로 숫자를 보여주고 있어서(도넛 중앙/금일 줄/온도), 상단 4칸 줄을 통째로 제거해도 정보 손실 없음 확인. 예약현황 미니카드만 가동률 숫자가 빠져있어서 그것만 추가, 날씨 특보 배지도 날씨 미니카드 쪽으로 이동. 이제 각 지표가 정확히 한 번씩만 나오고 화면도 훨씬 짧아짐 (안 쓰는 `.dash-stat-row`/`.stat-tile`/`.stat-label` CSS도 정리).
  - 매출현황 도넛이 작고 항목명↔퍼센트 간격이 너무 멀다는 피드백 → `.mini-legend-name`의 `flex:1`이 원인(카드 끝까지 늘어나게 만들던 것) 제거, 도넛 84px→104px 확대
  - **배포 이슈 1건**: 커밋 하나가 GitHub→Vercel 자동 배포 웹훅을 못 타서(일회성) Deployments 목록에 아예 안 뜬 적 있음 — `git ls-remote`로 GitHub엔 정상적으로 올라갔음을 먼저 확인한 후, 빈 커밋(`--allow-empty`)을 다시 푸시해서 강제로 재트리거해 해결. 원인 불명이라 다시 발생하면 같은 방법 사용.
  - 매 변경마다 실제 배포 URL의 서빙된 CSS/HTML을 직접 curl로 확인해 반영 여부 검증(캐시 문제 아님을 `X-Vercel-Cache`/`Cache-Control` 헤더로도 확인), `/api/health` 재확인. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **상세화면 뒤로가기 버튼 추가** (2026-09-17) — 하단 탭바에서 "요약"을 뺀 뒤로 상세 화면(매출/예약/자금/그린피/날씨)에서 요약 화면으로 돌아갈 방법이 없었음(탭바는 이 5개 화면끼리만 이동, 데스크톱 사이드바엔 Dashboard가 있어서 문제없었지만 모바일엔 없었음). 5개 상세 화면 subheader 위에 "‹ 스톤게이트CC" 버튼 추가(`go("dashboard")`). 검증 중 로컬 개발서버(`next dev`)의 Next.js 자체 개발자도구 플로팅 버튼("N" 아이콘, 배포판엔 없음)이 하필 모바일 탭바 첫 번째("매출") 위치와 겹쳐서 자동화 테스트 클릭을 가로채는 걸 발견 — 실제 버그 아님을 확인하기 위해 별도 포트(3001)에서 프로덕션 빌드(`next build && next start`)로 재검증, 정상 작동 확인.
- ✅ **글자 크게 보기 기능 추가** (2026-09-17) — 대표님이 노안이 있어 작은 글씨가 불편하다는 요청. 상단 유틸리티 바(데스크톱/모바일 공통)에 "가" 버튼 추가, 누를 때마다 기본→118%→138%→기본 순환. `.app-shell`에 CSS `zoom`을 적용해 글자뿐 아니라 아이콘·카드·여백까지 전부 비례 확대(폰트만 커지는 게 아니라 실제 화면 확대처럼 동작). 기기별로 `localStorage`에 저장해 새로고침해도 유지(단, 하이드레이션 불일치 방지를 위해 최초 렌더는 항상 "기본"이고 마운트 이후 저장값으로 갱신 — 관련 React lint 룰은 의도된 예외로 처리). 세 단계 전부 모바일(390px)·데스크톱에서 오버플로우·레이아웃 깨짐 없음을 스크린샷으로 확인(최대 단계에서 3px의 미세한 반올림 오차만 있고 시각적으로는 문제없음), 새로고침 후 설정 유지도 확인.
- ✅ **모바일 상세화면 1차 디자인 다듬기** (2026-09-21) — 5개 상세 화면(매출/예약/자금/그린피/날씨)을 실제 모바일 뷰포트(390px)로 전부 스크린샷 검토 후 눈에 띄는 문제 2건만 우선 수정, 예약/그린피/날씨는 이미 양호해서 그대로 둠
  - **매출현황**: KPI 카드 3개(매출액/객단가/예약팀수·가동률)가 `.dash-grid`/`.sim-grid`와 같이 모바일에서 무조건 1열로 강제되어 세로로 길게 쌓이던 것 — `.kpi-row`만 그 규칙에서 빼고 패딩·폰트만 살짝 축소, 데스크톱처럼 3칸 한 줄로 유지되도록 수정
  - **자금현황**: "은행별 자금 흐름" 표가 다른 화면과 다르게 원자값(예: 715,494,368)을 그대로 출력해서 가독성이 떨어지고 가로 스크롤 시 마지막 열이 잘려 보이던 것 — 이미 앱 전체에서 쓰는 `fmtCompact()`(억/만 축약)로 통일, 스크롤 없이 5개 열 전부 한 화면에 들어오도록 개선
  - **검증 방법**: 이번에도 로컬 개발서버(`next dev`)의 Next.js 개발자도구 플로팅 버튼이 모바일 탭바 첫 번째 항목과 겹쳐 자동화 클릭을 가로채는 문제가 재발 — 이제 이 패턴이 반복 확인된 만큼, 앞으로 모바일 탭 전환이 필요한 스크린샷 검증은 처음부터 `next build && next start`(별도 포트)로 진행하는 게 낫다는 걸 알아둘 것. 실제로 프로덕션 빌드로 전환 후 5개 화면 전부 재스크린샷, 수정 전/후 비교로 개선 확인. 타입체크+lint+프로덕션 빌드 전부 통과.
  - **병합 이슈**: 작업 중간에 집 PC 세션에서 푸시한 커밋(글자 크게 버튼의 "가" 텍스트를 돋보기 아이콘으로 교체)이 원격에 먼저 올라와 있어 `git push`가 한 번 거부됨 — `git fetch`로 내용 확인(`DashboardClient.tsx` 1줄만 변경, 겹치는 부분 없음) 후 병합, 충돌 없이 정상 처리.
- ✅ **Dashboard 요약 화면에 날짜 스트립 선택기 추가** (2026-09-21)
  - 배경: 사용자가 에버랜드 앱의 "일별 운영정보" 화면(요일+날짜 칩을 가로로 나열, 선택된 날짜가 알약 모양으로 강조)을 참고 이미지로 보여주며 비슷한 UI를 요약 화면에 적용하고 싶다고 요청 — 단, 우리는 미래 일정이 아니라 과거 실적 조회 용도라 **날짜 방향을 반대로**(오늘이 맨 오른쪽에 오고 기본 선택, 미래 날짜 없음) 적용
  - 이 작업을 하며 기존 문제도 하나 발견: 요약 화면의 매출/예약/자금 미니카드가 각자 "가장 최근 업로드된 날짜"를 따로 보여주고 있어서, 예를 들어 매출은 9/13 기준인데 자금은 8/31 기준인 식으로 카드마다 날짜가 은근슬쩍 달랐음 — 날짜 스트립 도입으로 이 문제가 자연스럽게 해결됨(선택한 날짜 하나로 4개 카드 전부 통일)
  - 최근 14일을 가로 스크롤 스트립으로 표시(오늘이 오른쪽 끝, 기본 선택, 페이지 로드 시 자동으로 오른쪽 끝까지 스크롤). 매출/예약/자금 카드는 선택된 날짜로 조회, 그린피 카드는 선택된 날짜의 "연월"로 조회(그린피는 월 단위 데이터라서). **날씨 카드만 예외** — 과거 날씨는 저장하지 않는 구조라 항상 실시간 현재 날씨를 보여줌(날짜 스트립과 무관)
  - `web/src/app/page.tsx`에서 이미 계산은 해두고 안 넘기던 `cashFlows`(자금일보 전체 이력)를 `DashboardClient`로 전달하도록 추가 — 예전엔 최신 1건(`latestCashFlow`)만 넘겨서 특정 과거 날짜 조회가 애초에 불가능했음
  - **검증**: 이번에도 `next dev`의 개발자도구 플로팅 버튼이 모바일 탭바와 겹쳐 자동화 클릭을 방해해서 프로덕션 빌드(별도 포트)로 검증 — 기본 상태(오늘 선택, 실제 데이터 있음/없음 정확히 반영)와 14일 전 날짜 클릭 시 4개 카드가 전부 그 날짜 기준으로 정확히 바뀌는 것(매출 있음/자금 없음/그린피 미입력 등 실제 데이터 상태 그대로 반영)을 데스크톱·모바일 스크린샷으로 확인, 오버플로우 없음. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **날짜 스트립 후속 수정 — 실기기에서 오늘 날짜 안 보이던 문제** (2026-09-21) — 배포 직후 사용자가 실제 폰으로 확인하니, 최근 14일 스크롤 스트립을 페이지 로드 시 JS로 끝까지 자동 스크롤하던 방식이 실기기에서 안정적으로 안 먹혀서 오늘 날짜를 보려면 옆으로 스크롤해야 했음(헤드리스 테스트에서는 문제없이 통과했었음). 근본적으로 스크롤에 의존하지 않도록 **5일 고정 + 칸마다 `flex:1`로 폭에 꽉 차게 균등 배치**하는 방식으로 교체 — 스크롤 자체가 필요 없어져서 "자동 스크롤이 제때 실행되는지"에 의존하지 않게 됨(대신 14일치 과거 조회는 포기, 필요하면 나중에 "더보기" 같은 방식으로 재검토). 375px 등 좁은 화면에서도 5칸이 오버플로우 없이 정확히 들어맞는 것 확인.
- ✅ **매출현황 "일간" 탭에 날짜 검색 기능 추가** (2026-09-21) — 일간 탭이 항상 "가장 최근 업로드된 날짜"만 보여주고 다른 날짜를 조회할 방법이 없었음. 기간 탭(일간/주간/월간/연간) 바로 아래에 **‹ 이전 / 날짜 입력(네이티브 date picker) / 다음 ›** 행 추가(일간 탭에서만 표시), 날짜를 직접 입력하거나 하루씩 이동 가능, 미래 날짜는 입력창 `max`와 "다음" 버튼 비활성화로 차단(대시보드 요약의 날짜 스트립과 동일한 원칙). 날짜를 고르면 "최신으로" 버튼이 나타나 원래 상태로 되돌릴 수 있음. 주간/월간/연간 탭은 기존처럼 항상 최신 기준 그대로 둠(이번 요청은 "일간"에 한정).
  - **검증**: 프로덕션 빌드로 "이전" 버튼 5번 클릭 vs 날짜 입력창에 같은 날짜(2026-09-08) 직접 입력 — 둘 다 동일한 실제 값(₩47,750,000)으로 일치하는 것 확인, "최신으로" 클릭 시 원래 최신(9/13, ₩69,580,000)으로 정확히 복귀 확인. 모바일 오버플로우 없음. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **매출현황에 커스텀 기간 검색 탭 추가** (2026-09-21) — 일간 탭에 날짜 검색을 추가했더니, 바로 이어서 "9/1~9/10처럼 임의의 기간도 검색하고 싶다"는 요청 → 일간/주간/월간/연간 옆에 **"기간" 탭**을 하나 더 추가. 주간 탭은 항상 "최신 업로드 기준 최근 7일"로 고정이라 임의 구간을 못 봤는데, 기간 탭은 시작일·종료일 두 날짜 입력으로 완전히 임의의 구간을 조회 가능(처음 열면 기본값으로 최근 7일 표시). 히어로/도넛/범례 등 기존 카드는 그대로 재사용(어차피 `periodDocs`/`periodSum` 기반이라 필터 로직만 추가하면 됨).
  - **검증**: 프로덕션 빌드로 9/1~9/10 선택 시 ₩568,830,000 나오는 것을, 그 10일치 실제 값을 손으로 직접 더해서 정확히 일치함을 확인(중간에 낀 9/1~9/10 전부 실제 데이터 있음). 375px 화면에서도 시작일·종료일 입력 두 개가 겹치거나 잘리지 않고 한 줄에 정상 표시됨. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **매출현황 기간 필터를 "기간" 검색 하나로 단순화** (2026-09-22) — 사용자가 일간/주간/월간/연간 탭과 새로 만든 "기간"(범위) 검색이 기능적으로 중복된다고 지적 → 4개 고정 탭(일간/주간/월간/연간)과 그에 딸린 날짜 이동(‹/›) UI를 전부 제거하고, **시작일~종료일 두 날짜 입력만 항상 노출**되는 방식으로 통일. 처음 열면 기본값으로 최근 7일(최신 업로드 기준)이 표시되는 동작은 그대로 유지. `SalesPeriod` 타입, `salesPeriod`/`pickedSalesDate` state, `periodDocs`의 일/주/월/연 분기 로직, `period-tabs`/`tab-btn`/`day-search-row`/`day-search-btn`/`day-search-reset` CSS를 전부 제거(죽은 코드 정리).
  - **검증**: 프로덕션 빌드(별도 포트)로 매출현황 화면 모바일(390px) 스크린샷 확인 — 탭 UI 없이 날짜 범위 입력만 상단에 바로 보이고, 기본 7일 범위(9/7~9/13, ₩444,564,000) 정상 표시, 오버플로우 없음, 콘솔 에러 없음. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **휴대폰 뒤로가기(하드웨어 버튼/제스처) 수정** (2026-09-22) — 사용자가 "백버튼이 잘 안 먹히는것 같아"라고 리포트. 원인 확인: 상세화면(매출/예약/자금/그린피/날씨) ↔ 요약화면 전환이 전부 React state(`view`)만 바꾸고 URL/브라우저 히스토리를 전혀 건드리지 않아서, 화면 안의 "‹ 스톤게이트CC" 버튼은 항상 잘 작동했지만 **휴대폰 자체의 뒤로가기(제스처/버튼)는 히스토리에 되돌릴 게 없어 그냥 사이트를 벗어나 버렸음** — 이게 진짜 원인.
  - 수정: `DashboardClient.tsx`에 히스토리 동기화 로직 추가 — 요약화면이 아닌 상세화면으로 처음 진입할 때 `history.pushState`로 엔트리 1개를 쌓고(탭바로 상세화면끼리 이동할 땐 추가로 쌓지 않고 깊이 1을 유지), `popstate`(뒤로가기 감지) 발생 시 무조건 요약화면으로 이동하도록 처리 — 기존 "‹" 버튼과 동일한 동작으로 통일. 인앱 "‹" 버튼으로 요약화면에 돌아갈 때도 쌓인 엔트리를 `history.back()`으로 같이 정리해서 히스토리가 계속 쌓이지 않도록 함.
  - **검증**: 프로덕션 빌드로 (1) 상세화면 진입 후 브라우저 뒤로가기 → 요약화면 복귀, (2) 인앱 "‹" 버튼으로 복귀 후 다시 진입+뒤로가기 반복해도 정상 동작(엔트리 안 쌓임), (3) 탭바로 매출→그린피처럼 상세화면끼리 바로 이동한 뒤 뒤로가기 한 번만 눌러도 곧장 요약화면으로 감(중간 상세화면에 안 걸림) — 전부 Playwright로 시뮬레이션해 확인, 콘솔 에러 없음. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **Dashboard 요약화면의 예약현황 미니카드를 막대그래프 → 큰 숫자로 변경** (2026-09-22) — 사용자 요청: "그래프 보다는 큰 숫자로 66/126 52.3% 이런식으로만 보여주면 될 것 같아". 최근 7일 미니 막대그래프(`mini-bar-row`)를 제거하고 그 자리에 예약팀수/전체타임(예: `49/88`)을 큰 글씨로, 가동률을 소수점 1자리(예: `55.7%`)로 바로 아래 표시. 관련 죽은 CSS(`.mini-bar-row`/`.mini-bar-col`/`.mini-bar`/`.mini-bar-label`, 더는 안 쓰는 `.stat-value`) 정리.
  - **검증**: 실제 예약 데이터가 있는 날짜(2026-08-31, Playwright `page.clock`으로 브라우저 시각을 그 날짜로 고정해 확인)로 프로덕션 빌드 스크린샷 확인 — "49/88" / "55.7%"가 그래프 없이 정확히 표시됨. 타입체크+lint+프로덕션 빌드 전부 통과.
- ✅ **자금현황에 "총 보유자금" 큰 숫자 추가 + 더존 실제 파일 2종 검토** (2026-09-22) — 사용자가 "대표님이 자금 시재 부분에 궁금증이 크셔"라며, 대표님이 실제로 관리하시는 더존 파일 2개(`grid1_...xlsx`=예금현황, `ACC0020_Tab1_Grid_...xlsx`=자금일보 샘플)를 EXCEL 폴더에 넣어주셔서 검토.
  - **중요 발견**: 처음엔 "정기예적금/현금이 기존 스키마에 빠져있나?" 의심했으나, 실제 프로덕션 원본 파일(`자금일보_26년 8월.xlsx`)의 원시 XML을 직접 까본 결과 — 기존 `cashFlow.ts` 파서가 이미 "보관 시재"/"프론트 매출"/"프론트 시재"(현금 서랍) 항목까지 은행 계좌들과 동일하게 개별 라인으로 파싱해서 `총합계`(=`todayBalance`)에 전부 포함하고 있었음. 즉 **데이터는 이미 완전했고, 문제는 그 총합계가 "금일"이라는 이름으로 4칸짜리 전일/입금/출금/금일 행 안에 다른 숫자들과 동등한 크기로만 묻혀 있어서 안 보였던 것** — 새 파서나 업로드 흐름 추가 없이 표시 방식만 바꾸면 되는 문제였음.
  - 수정: 자금현황 상세화면 최상단 카드를 매출현황과 같은 `hero`(큰 숫자) 스타일로 교체 — "총 보유자금 ₩321,907,935" + 그 아래 "전일 8.8억 · 입금 5,231만 · 출금 6.1억" 요약. 계좌별 잔액 막대 목록은 별도 카드로 분리, "은행별 자금 흐름" 표 카드는 그대로 유지. Dashboard 요약 화면의 자금현황 미니카드도 같은 원칙으로 "전일/금일" 2줄 미니 행 → "3.2억 / 총 보유자금 · 전일 8.8억" 큰 숫자 한 줄로 교체(예약현황 미니카드를 큰 숫자로 바꾼 것과 통일된 패턴). 안 쓰는 `.mini-flow-row` CSS 정리.
  - **검토했지만 이번엔 반영 안 한 것**: `grid1_...xlsx`(예금현황) 파일에만 있는 "정기예금/적금 만기일" 정보는 지금 일일 업로드 파일엔 없는 항목이라, 다음 우선순위(대표님이 선택 시)로 남겨둠 — 실제 정기예적금 잔액이 생기면 그때 이 파일 형식을 새로 파싱해서 "만기 D-day" 알림 기능으로 연결할 수 있음.
  - **검증**: 프로덕션 빌드로 실제 데이터(2026-08-31 기준, ₩321,907,935 — 기존에 확인된 값과 100% 일치) 모바일 스크린샷 확인, 오버플로우 없음, 데이터 없는 날짜의 빈 상태도 정상. 타입체크+lint+프로덕션 빌드 전부 통과.
- ⬜ **다음으로 할 것**: `UPLOAD_PIN` 실제 값으로 변경(현재 `1234` 임시값), GitHub 저장소 공개/비공개 최종 결정, (선택) 날씨 자동갱신을 위한 Vercel Cron 설정(`/api/weather/refresh`를 주기적으로 호출 — 지금은 대시보드 방문 시에만 갱신됨), (선택) 정기예금/적금 만기일 알림 기능(예금현황 파일 형식 신규 파싱 필요)
- 참고: 2번째 달부터는 그린피 입력 폼이 "전달 저장값"을 기본값으로 불러오면 더 편할 텐데, 지금은 항상 9월 안내문 기준 고정값으로 프리필됨 (개선 여지로 남겨둠)
- 참고: 로컬 개발 서버는 `cd web && npm run dev` → http://localhost:3000

### 4.3 아직 시작 안 한 계정 준비
- ✅ 기상청 API 키 발급 및 연동 완료 (위 §4.2 참고 — apihub.kma.go.kr, data.go.kr 아님)
- ✅ Vercel 배포 완료 (위 §4.2 참고) — https://golf-dashboard-vnzh.vercel.app
- GitHub 저장소 공개/비공개 여부 최종 결정 (현재 퍼블릭 유지 중)

---

## 5. Firebase 프로젝트 설정값 (비밀 아님 — Firebase 설계상 클라이언트에 공개돼도 안전한 값들)

```js
const firebaseConfig = {
  apiKey: "AIzaSyCycIosUfsrSlLfswIXl8r3HCeQGQaKVIA",
  authDomain: "stonegate-8f538.firebaseapp.com",
  projectId: "stonegate-8f538",
  storageBucket: "stonegate-8f538.firebasestorage.app",
  messagingSenderId: "887321860798",
  appId: "1:887321860798:web:6433e481735ce8017b1cef"
};
```
- 프로젝트 ID: `stonegate-8f538`
- Firestore 리전: `asia-northeast3` (서울)
- Firestore 보안 규칙: 프로덕션 모드로 시작 (현재 전부 차단 상태, `allow read, write: if false`) — 코드 구현 시 정식 규칙으로 교체 예정
- Storage: 미사용 (위 결정 참고)
- 서비스 계정 키: **발급 완료**, `web/.env.local`에 반영됨 (파일 자체는 로컬 전용, 절대 커밋 금지)

---

## 6. 중요 주의사항

- **`/EXCEL` 폴더는 `.gitignore`로 제외**되어 GitHub에 올라가지 않음 (실제 은행계좌번호·거래처·지급액 등 민감 데이터 포함).
- **Firebase 서비스 계정 키(JSON)도 `.gitignore`에 패턴 추가해둠** (`*firebase-adminsdk*.json`, `serviceAccountKey.json` 등) — 폴더에 파일이 도착해도 자동으로 커밋 대상에서 제외됨. 그래도 `git add` 전에 항상 `git status`로 한 번 더 확인할 것.
- 저장소(`github.com/inus209120-star/golf_dashboard`)는 **현재 퍼블릭**. `firebaseConfig` 값은 공개돼도 안전하지만, 서비스 계정 키·기상청 API 키 등 진짜 비밀값은 절대 이 저장소에 커밋하지 말 것 (전부 `.env.local`로만 관리).
- 화면 설계의 예약/매출/자금 수치는 전부 **예시 데이터**이며, 그린피 요금표만 실제 9월 안내문 수치 반영.

---

## 7. 모바일 대시보드 재설계 (진행 중 — 2026-09-14, 집 PC 세션)

**배경**: 대표님 이하 직원들이 실제로는 PC보다 모바일로 대시보드를 훑어보는 빈도가 높을 것으로 판단, 기존 `MainMobile.dc.html`(초기 버전, Main.dc.html의 최종 구조 미반영)을 재설계하기로 함. **아직 시안 단계이며 실제 `.dc.html` 파일에는 반영 안 됨.**

### 7.1 KPI 카드 5종 시안 (데스크톱)
- 매출/예약/자금/그린피/날씨를 한 줄 KPI 카드로 배치하는 A안(균등 5칸)·B안(히어로+보조4) 비교 시안 작성
- 게시 링크: https://claude.ai/code/artifact/0e662af9-e953-4583-b44e-44e5a214b5c1
- **A안(균등 5칸) 추천 확정** — 5개 지표가 병렬 관계(우열 없음)라는 이유. 사용자 "나쁘지 않네"로 긍정 반응, 정식 채택 여부는 아직 명시적 확답 전.
- 적용 시 기존 `.dash-stat-row`(4칸)와 `.dash-grid` 미니카드 사이에 이 5칸을 끼워 넣는 구조 제안 (문서 내 가이드 포함)

### 7.2 모바일 요약 화면 시안
- 게시 링크: https://claude.ai/code/artifact/7184db84-fcf5-4e25-9199-2ee28da3ec35
- 376px 실제 폰 폭 프레임으로 제작. 핵심 변경사항:
  - **하단 탭바 신규 추가** (요약/매출/예약/자금/그린피/날씨 6개 아이콘) — 데스크톱 사이드바에 대응하는 모바일 내비게이션. 기존엔 이런 페이지 이동 수단 자체가 없었음(§1 미해결 이슈 참고)
  - KPI 5칸을 데스크톱 A안과 동일 문법으로 2열 반응형 배치 (홀수라 남는 날씨 카드는 한 줄 전체 사용)
  - 예약 캘린더·매출 막대 등 무거운 위젯은 요약 화면에서 제거하고 각 상세 탭으로 이동, 요약엔 "최근 7일 매출 추이" 미니 그래프만 남겨 스크롤 없이 한 화면에 보이게 정리
- **작업 범위를 "대시보드 요약 화면부터"로 명시적으로 한정** — 나머지 5개 탭(매출/예약/자금/그린피/날씨) 상세 화면은 다음 단계로 남겨둠

### 7.3 다음에 할 것
- 사용자 최종 승인 시: 이 모바일 요약 시안을 실제 `design/MainMobile.dc.html`에 반영
- 이어서 나머지 5개 탭 상세 화면도 같은 탭바 기준으로 모바일 재설계
- 데스크톱 KPI 5칸(A안)도 실제 `design/Main.dc.html`에 반영할지 결정 필요
- (참고) 이 세션은 "확인용" 집 PC에서 진행 — git 저장소로 연결 안 된 상태라 위 시안들은 Claude 아티팩트 링크로만 존재함. 회사 PC에서 실제 파일 반영 작업 시 이 두 링크를 열어서 참고할 것.

---

## 8. (참고) DART 공시 기반 동종업계 벤치마크 조사 — 2026-09-14

대표님이 스톤게이트(대중형) 실적을 유명 골프장과 비교하고 싶어해서 금감원 DART 기반 자료 조사 진행.
- **오션디앤씨(스톤게이트 운영사) 자체는 DART 공시 대상 아님** (검색상 조회 결과 없음 — 정확한 법인 등기명 재확인 필요할 수도)
- **확실한 비교 자료**: 골프존카운티(다수 대중형 코스 운영사) 2022~2025년 매출 1,775~1,997억, 영업이익 156~492억
- **업계 평균(한국레저산업연구소 레저백서)**: 대중형 골프장 평균 매출 180억원(2024), 영업이익률 36.9%
- 스카이72/베어즈베스트청라/잭니클라우스GC코리아 등 개별 유명 대중형 골프장은 정확한 법인 등기명 특정 실패로 데이터 확보 못함 — 필요시 OpenDART(opendart.fss.or.kr) 무료 API 키 발급받으면 더 정확히 조회 가능(금감원 공식 API, 기상청 apihub 키 발급 때와 같은 방식)
- 스톤게이트 실제 실적은 사용자가 엑셀로 제공 예정 — 받으면 위 벤치마크와 함께 대시보드 참고 자료로 정리 예정 (아직 미착수)
